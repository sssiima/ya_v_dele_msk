// import { useTelegram } from '../hooks/useTelegram'

const ProfilePage = () => {
  // const { user } = useTelegram()

  return (
    <div className="min-h-screen bg-white">
      {/* Страница полностью пустая с белым фоном */}
    </div>
  )
}

export default ProfilePage