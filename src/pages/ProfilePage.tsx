// import { useTelegram } from '../hooks/useTelegram'

const ProfilePage = () => {
  // const { user } = useTelegram()

  return (
    <div className="min-h-screen bg-brand flex items-center justify-center text-center">
      {/* Страница полностью пустая с белым фоном */}
      <h1 className="text-white text-2xl font-bold">Вся информация о профиле скоро появится</h1>
    </div>
  )
}

export default ProfilePage